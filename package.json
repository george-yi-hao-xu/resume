{
  "name": "resume",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "export:boss": "node --no-warnings scripts/export-boss-markdown.mjs",
    "export:boss:bilingual": "node --no-warnings scripts/export-boss-markdown.mjs --lang=bilingual"
  },
  "dependencies": {
    "@resume/one-col": "workspace:*"
  },
  "devDependencies": {
    "sass": "^1.77.0",
    "vite": "^8.0.16"
  }
}
